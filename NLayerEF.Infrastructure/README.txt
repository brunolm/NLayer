/* 
 * This project defines the Factory to retrieve the DbContext and
 * contains DbContext implementation
 * 
 * The transformation file (.tt) was modified to include the domain namespace,
 * change the context constructor to internal and to change the edmx location.
 * There is a TODO, which is to modify the constructor to read the active connectionstring
 * from the configuration file instead of being hardcoded.
 */